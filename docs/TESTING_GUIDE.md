# 🧪 دليل الاختبار الشامل - Platform Billing System

## 📋 قائمة الاختبار

---

## ✅ **المتطلبات الأولية:**

### 1. تشغيل Backend:
```bash
cd backend
npm start

# تأكد من ظهور:
✅ [MARKETPLACE] Platform Subscription routes loaded
✅ [SUPER-ADMIN] Platform Management routes loaded
✅ [SUPER-ADMIN] Marketplace Management routes loaded
✅ [BILLING-CRON] Billing Cron Service started
```

### 2. تشغيل Frontend:
```bash
cd frontend
npm run dev
```

### 3. تسجيل الدخول:
- **للشركات:** أي حساب شركة
- **للسوبر أدمن:** حساب بصلاحيات Super Admin

---

## 🧪 **الاختبارات:**

---

## **1️⃣ Platform Fee System (رسوم المنصة)**

### **أ) للشركات:**

#### **اختبار 1: عرض الخطط المتاحة**
```
URL: /subscription/plans
الخطوات:
1. افتح الصفحة
2. تأكد من ظهور 3 خطط (BASIC, PRO, ENTERPRISE)
3. تأكد من ظهور الأسعار (99 ج، 199 ج، 499 ج)
4. تأكد من ظهور الميزات لكل خطة

✅ النتيجة المتوقعة: عرض جميع الخطط بشكل صحيح
```

#### **اختبار 2: عرض الاشتراك الحالي**
```
URL: /subscription/my-subscription
الخطوات:
1. افتح الصفحة
2. تأكد من ظهور الخطة الحالية
3. تأكد من ظهور تاريخ الفاتورة القادمة
4. تأكد من ظهور سجل الفواتير

✅ النتيجة المتوقعة: عرض معلومات الاشتراك بشكل كامل
```

#### **اختبار 3: مراقبة الاستخدام**
```
URL: /subscription/usage
الخطوات:
1. افتح الصفحة
2. تأكد من ظهور الموظفين (X/Y)
3. تأكد من ظهور المنتجات (X/Y)
4. تأكد من ظهور الطلبات (X/Y)
5. تأكد من ظهور Progress bars

✅ النتيجة المتوقعة: عرض الاستخدام مقابل الحدود
```

### **ب) للسوبر أدمن:**

#### **اختبار 4: إدارة اشتراكات المنصة**
```
URL: /super-admin/platform-subscriptions
الخطوات:
1. افتح الصفحة
2. تأكد من ظهور جميع اشتراكات الشركات
3. تأكد من ظهور Statistics Cards (إجمالي، نشط، MRR)
4. جرب الفلترة حسب الحالة
5. جرب الفلترة حسب الخطة
6. جرب البحث عن شركة

✅ النتيجة المتوقعة: عرض وفلترة الاشتراكات بنجاح
```

#### **اختبار 5: نظرة عامة على الفواتير**
```
URL: /super-admin/billing-overview
الخطوات:
1. افتح الصفحة
2. تأكد من ظهور إجمالي الإيرادات
3. تأكد من ظهور MRR
4. تأكد من ظهور الإيرادات حسب النوع
5. تأكد من ظهور الإيرادات حسب الخطة
6. تأكد من ظهور الدفعات الفاشلة

✅ النتيجة المتوقعة: عرض تحليلات الإيرادات بشكل كامل
```

---

## **2️⃣ Marketplace Apps System**

### **أ) للشركات:**

#### **اختبار 6: تصفح الأدوات**
```
URL: /marketplace
الخطوات:
1. افتح الصفحة
2. تأكد من ظهور جميع الأدوات المتاحة
3. تأكد من ظهور الأسعار
4. جرب الفلترة حسب الفئة
5. جرب البحث عن أداة

✅ النتيجة المتوقعة: عرض جميع الأدوات بشكل صحيح
```

#### **اختبار 7: تفاصيل أداة**
```
URL: /marketplace/:slug
الخطوات:
1. اضغط على أي أداة
2. تأكد من ظهور الوصف الكامل
3. تأكد من ظهور الميزات
4. تأكد من ظهور السعر
5. تأكد من وجود زر "تفعيل"

✅ النتيجة المتوقعة: عرض تفاصيل الأداة بالكامل
```

#### **اختبار 8: أدواتي المفعلة**
```
URL: /my-apps
الخطوات:
1. افتح الصفحة
2. تأكد من ظهور الأدوات المفعلة
3. تأكد من ظهور حالة كل أداة (نشط/معلق)
4. تأكد من ظهور تاريخ التجديد
5. جرب إلغاء تفعيل أداة

✅ النتيجة المتوقعة: عرض وإدارة الأدوات المفعلة
```

### **ب) للسوبر أدمن:**

#### **اختبار 9: إدارة الأدوات**
```
URL: /super-admin/marketplace-management
Tab: الأدوات
الخطوات:
1. افتح التبويب
2. تأكد من ظهور جميع الأدوات
3. جرب إضافة أداة جديدة
4. جرب تعديل أداة موجودة
5. تأكد من ظهور عدد التفعيلات

✅ النتيجة المتوقعة: CRUD كامل للأدوات
```

---

## **3️⃣ Usage-Based Pricing**

### **اختبار 10: إدارة قواعد التسعير**
```
URL: /super-admin/marketplace-management
Tab: قواعد التسعير
الخطوات:
1. افتح التبويب
2. تأكد من ظهور جميع القواعد
3. تأكد من ظهور الأسعار (0.10-1 ج)
4. جرب تعديل سعر قاعدة
5. جرب إضافة قاعدة جديدة

✅ النتيجة المتوقعة: إدارة قواعد التسعير بنجاح
```

#### **اختبار 11: تتبع الاستخدام**
```
URL: /subscription/usage
الخطوات:
1. افتح الصفحة
2. تأكد من ظهور سجل الاستخدام
3. تأكد من حساب التكلفة بشكل صحيح
4. مثال: 100 طلب × 0.50 ج = 50 ج

✅ النتيجة المتوقعة: عرض الاستخدام والتكلفة
```

---

## **4️⃣ Bundles System**

### **أ) للشركات:**

#### **اختبار 12: تصفح الباقات**
```
URL: /marketplace/bundles
الخطوات:
1. افتح الصفحة
2. تأكد من ظهور جميع الباقات
3. تأكد من حساب التوفير بشكل صحيح
4. مثال: باقة بـ 120 ج بدلاً من 150 ج = وفر 30 ج (20%)
5. تأكد من ظهور الأدوات المتضمنة
6. جرب زر "اشترك الآن"

✅ النتيجة المتوقعة: عرض الباقات مع حساب التوفير
```

### **ب) للسوبر أدمن:**

#### **اختبار 13: إدارة الباقات**
```
URL: /super-admin/marketplace-management
Tab: الباقات
الخطوات:
1. افتح التبويب
2. تأكد من ظهور جميع الباقات
3. جرب إضافة باقة جديدة
4. جرب تعديل باقة موجودة
5. تأكد من اختيار الأدوات المتضمنة

✅ النتيجة المتوقعة: CRUD كامل للباقات
```

---

## **5️⃣ Enterprise Plans**

### **اختبار 14: إدارة خطط Enterprise**
```
URL: /super-admin/marketplace-management
Tab: خطط Enterprise
الخطوات:
1. افتح التبويب
2. تأكد من ظهور جميع الخطط المخصصة
3. جرب إضافة خطة جديدة لشركة
4. جرب تعديل الأسعار المخصصة
5. جرب تعديل الحدود المخصصة
6. تأكد من حفظ معلومات مدير الحساب

✅ النتيجة المتوقعة: إدارة خطط Enterprise بنجاح
```

---

## **6️⃣ Wallet System**

#### **اختبار 15: المحفظة**
```
URL: /wallet
الخطوات:
1. افتح الصفحة
2. تأكد من ظهور الرصيد الحالي
3. تأكد من ظهور سجل الحركات
4. تأكد من ظهور أنواع الحركات:
   - DEPOSIT (إيداع)
   - DEBIT (خصم - رسوم المنصة)
   - DEBIT (خصم - اشتراك أداة)
   - DEBIT (خصم - استخدام)
5. جرب شحن المحفظة

✅ النتيجة المتوقعة: عرض وإدارة المحفظة
```

---

## **7️⃣ Billing Cron Service**

#### **اختبار 16: الخصم التلقائي**
```
ملاحظة: هذا الاختبار يتطلب انتظار أو تعديل وقت الـ Cron

الخطوات:
1. تأكد من تشغيل Backend
2. انتظر حتى الساعة 2 صباحاً (أو عدل الوقت في billingCronService.js)
3. تحقق من Logs في Console
4. تحقق من خصم الرسوم من المحافظ
5. تحقق من تحديث nextBillingDate
6. تحقق من تسجيل العمليات في BillingHistory

✅ النتيجة المتوقعة: خصم تلقائي شهري ناجح
```

---

## **8️⃣ APIs Testing (Postman/Thunder Client)**

### **اختبار 17: Platform Subscription APIs**
```
GET /api/v1/platform-subscription/current
Headers: Authorization: Bearer {token}

✅ النتيجة المتوقعة: 200 OK + بيانات الاشتراك
```

### **اختبار 18: Marketplace Management APIs**
```
GET /api/v1/super-admin/marketplace-management/apps
Headers: Authorization: Bearer {token}

✅ النتيجة المتوقعة: 200 OK + قائمة الأدوات
```

### **اختبار 19: Bundles APIs**
```
GET /api/v1/super-admin/marketplace-management/bundles
Headers: Authorization: Bearer {token}

✅ النتيجة المتوقعة: 200 OK + قائمة الباقات
```

### **اختبار 20: Pricing Rules APIs**
```
GET /api/v1/super-admin/marketplace-management/pricing-rules
Headers: Authorization: Bearer {token}

✅ النتيجة المتوقعة: 200 OK + قائمة القواعد
```

---

## **9️⃣ Database Testing**

### **اختبار 21: التحقق من Models**
```sql
-- التحقق من وجود جميع الجداول
SHOW TABLES LIKE '%Platform%';
SHOW TABLES LIKE '%Bundle%';
SHOW TABLES LIKE '%Enterprise%';

-- التحقق من البيانات
SELECT * FROM PlatformSubscription LIMIT 5;
SELECT * FROM PlanLimit;
SELECT * FROM AppBundle;
SELECT * FROM EnterprisePlan;
SELECT * FROM BillingHistory ORDER BY createdAt DESC LIMIT 10;

✅ النتيجة المتوقعة: جميع الجداول موجودة والبيانات صحيحة
```

---

## **🔟 Integration Testing**

### **اختبار 22: السيناريو الكامل**
```
السيناريو: شركة جديدة تشترك في المنصة

الخطوات:
1. إنشاء شركة جديدة
2. يتم إنشاء PlatformSubscription تلقائياً (BASIC)
3. يتم إنشاء CompanyWallet برصيد 0
4. الشركة تشحن المحفظة بـ 500 ج
5. الشركة تفعل أداة "WhatsApp Business" (50 ج/شهر)
6. الشركة تستخدم الميزات:
   - 100 رسالة WhatsApp × 0.10 ج = 10 ج
   - 50 طلب × 0.50 ج = 25 ج
7. الرصيد الآن = 500 - 50 - 10 - 25 = 415 ج
8. في أول الشهر القادم:
   - خصم رسوم المنصة: 415 - 99 = 316 ج
   - خصم رسوم WhatsApp: 316 - 50 = 266 ج
9. التحقق من جميع العمليات في:
   - WalletTransaction
   - BillingHistory
   - AppUsageLog

✅ النتيجة المتوقعة: جميع العمليات تتم بنجاح والأرصدة صحيحة
```

---

## **📊 Performance Testing**

### **اختبار 23: الأداء**
```
الخطوات:
1. افتح DevTools → Network
2. افتح صفحة /marketplace
3. تحقق من وقت التحميل (يجب أن يكون < 2 ثانية)
4. افتح صفحة /super-admin/marketplace-management
5. تحقق من وقت تحميل البيانات

✅ النتيجة المتوقعة: تحميل سريع للصفحات
```

---

## **🔒 Security Testing**

### **اختبار 24: الصلاحيات**
```
الخطوات:
1. سجل دخول كمستخدم عادي (ليس Super Admin)
2. حاول الوصول لـ /super-admin/marketplace-management
3. يجب أن يتم رفض الوصول (401 أو redirect)

✅ النتيجة المتوقعة: حماية صفحات Super Admin
```

---

## **✅ Checklist النهائي:**

- [ ] جميع الصفحات تفتح بدون أخطاء
- [ ] جميع APIs تعمل بشكل صحيح
- [ ] الخصم التلقائي يعمل
- [ ] حساب الأسعار صحيح
- [ ] سجل الحركات يظهر بشكل صحيح
- [ ] الفلترة والبحث يعملان
- [ ] الصلاحيات محمية
- [ ] لا توجد أخطاء في Console
- [ ] Database متسق
- [ ] الأداء جيد

---

## **🐛 الأخطاء الشائعة وحلولها:**

### **خطأ 404 في APIs:**
```
الحل: إعادة تشغيل Backend
cd backend
npm start
```

### **خطأ في تحميل الصفحات:**
```
الحل: مسح Cache
Ctrl+Shift+R (Hard Reload)
```

### **خطأ في Database:**
```
الحل: تشغيل Prisma Generate
cd backend
npx prisma generate
npx prisma db push
```

---

## **📝 ملاحظات:**

1. **Cron Job:** يعمل يومياً الساعة 2 صباحاً
2. **Decimal Types:** تأكد من تحويل Decimal إلى Number في Frontend
3. **Token:** تأكد من وجود token صالح في localStorage
4. **CORS:** تأكد من إعدادات CORS في Backend

---

## **✅ النتيجة النهائية:**

إذا نجحت جميع الاختبارات، فالنظام **جاهز 100% للإنتاج! 🚀**
