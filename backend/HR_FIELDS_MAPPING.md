# 🔍 HR Employee Fields Mapping

## 📋 مقارنة الحقول بين الفرونت إند والباك إند والـ Database

### ✅ الحقول المطلوبة والمحفوظة:

| الحقل | الفرونت إند | الباك إند | Database (User) | ملاحظات |
|------|------------|----------|-----------------|---------|
| `firstName` | ✅ | ✅ | ✅ | مطلوب |
| `lastName` | ✅ | ✅ | ✅ | مطلوب |
| `email` | ✅ | ✅ | ✅ | مطلوب، فريد |
| `phone` | ✅ | ✅ | ✅ | اختياري |
| `departmentId` | ✅ | ✅ | ✅ | اختياري |
| `positionId` | ✅ | ✅ | ✅ | اختياري |
| `hireDate` | ✅ | ✅ | ✅ | اختياري |
| `contractType` | ✅ | ✅ | ✅ | اختياري (FULL_TIME, PART_TIME, etc.) |
| `baseSalary` | ✅ | ✅ | ✅ | اختياري (Decimal) |
| `employeeNumber` | ❌ | ✅ | ✅ | يتم توليده تلقائياً إذا لم يتم توفيره |

### ⚠️ الحقول الإضافية من الفرونت إند (غير محفوظة):

| الحقل | الفرونت إند | الباك إند | Database | ملاحظات |
|------|------------|----------|----------|---------|
| `mobile` | ✅ | ❌ | ❌ | غير موجود في User model |
| `password` | ✅ | ❌ | ❌ | يتم توليده تلقائياً في الباك إند |
| `role` | ✅ | ❌ | ❌ | يتم تعيينه افتراضياً كـ 'AGENT' |
| `nationalId` | ✅ | ❌ | ❌ | غير موجود في User model |
| `dateOfBirth` | ✅ | ❌ | ❌ | غير موجود في User model |
| `gender` | ✅ | ❌ | ❌ | غير موجود في User model |
| `address` | ✅ | ❌ | ❌ | غير موجود في User model |
| `city` | ✅ | ❌ | ❌ | غير موجود في User model |
| `country` | ✅ | ❌ | ❌ | غير موجود في User model |

### 📝 ملاحظات مهمة:

1. **الحقول الفارغة**: يتم تحويل الحقول الفارغة (`''`) إلى `null` قبل الحفظ
2. **baseSalary**: يتم تحويله إلى `parseFloat()` قبل الحفظ
3. **hireDate**: يتم تحويله إلى `new Date()` قبل الحفظ
4. **employeeNumber**: إذا لم يتم توفيره، يتم توليده تلقائياً بصيغة `EMP00001`, `EMP00002`, إلخ
5. **email**: يتم تحويله إلى lowercase قبل الحفظ
6. **password**: يتم توليده تلقائياً إذا لم يتم توفيره

### 🔧 الكود الحالي:

#### الفرونت إند (`Employees.tsx`):
```typescript
const formData = {
  firstName: '',
  lastName: '',
  email: '',
  phone: '',
  mobile: '',           // ❌ غير مستخدم
  password: '',          // ❌ غير مستخدم (يتم توليده)
  role: 'AGENT',        // ❌ غير مستخدم (يتم تعيينه افتراضياً)
  departmentId: '',
  positionId: '',
  hireDate: '',
  contractType: 'FULL_TIME',
  baseSalary: '',
  nationalId: '',        // ❌ غير مستخدم
  dateOfBirth: '',      // ❌ غير مستخدم
  gender: '',           // ❌ غير مستخدم
  address: '',          // ❌ غير مستخدم
  city: '',             // ❌ غير مستخدم
  country: 'مصر',      // ❌ غير مستخدم
};
```

#### الباك إند (`employeeService.js`):
```javascript
const userData = {
  employeeNumber: data.employeeNumber,  // يتم توليده إذا لم يكن موجوداً
  departmentId: data.departmentId && data.departmentId !== '' ? data.departmentId : null,
  positionId: data.positionId && data.positionId !== '' ? data.positionId : null,
  hireDate: data.hireDate && data.hireDate !== '' ? new Date(data.hireDate) : null,
  contractType: data.contractType && data.contractType !== '' ? data.contractType : null,
  baseSalary: data.baseSalary && data.baseSalary !== '' ? parseFloat(data.baseSalary) : null,
  firstName: data.firstName,
  lastName: data.lastName,
  phone: data.phone && data.phone !== '' ? data.phone : null,
  avatar: data.avatar || null,
  companyId: companyId
};
```

### ✅ التحسينات المضافة:

1. **Logging**: تم إضافة logging لرؤية البيانات الواردة من الفرونت إند
2. **معالجة الحقول الفارغة**: يتم تحويل `''` إلى `null` بشكل صحيح
3. **التحقق من baseSalary**: يتم استخدام `parseFloat()` قبل الحفظ
4. **التحقق من hireDate**: يتم استخدام `new Date()` قبل الحفظ

### 🚀 الخطوات التالية (اختياري):

إذا كنت تريد حفظ الحقول الإضافية، يجب:
1. إضافة الحقول إلى `schema.prisma` في نموذج `User`
2. تشغيل migration
3. تحديث `employeeService.js` لحفظ هذه الحقول

